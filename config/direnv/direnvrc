#!/usr/bin/env bash
# shellcheck disable=SC2181

PYTHON_VERSION_PREFIX="${PYTHON_VERSION_PREFIX-}"
PYTHON_VERSIONS="${PYTHON_VERSIONS-"${HOME}/.asdf/installs/python"}"

NODE_VERSION_PREFIX="${NODE_VERSION_PREFIX-}"
NODE_VERSIONS="${NODE_VERSIONS-"${HOME}/.asdf/installs/nodejs"}"

RUBY_VERSION_PREFIX="${RUBY_VERSION_PREFIX-}"
RUBY_VERSIONS="${RUBY_VERSIONS-"${HOME}/.asdf/installs/ruby"}"

KUBECTL_VERSION_PREFIX="${KUBECTL_VERSION_PREFIX-}"
KUBECTL_VERSIONS="${KUBECTL_VERSIONS-"${HOME}/.asdf/installs/kubectl"}"

HELM_VERSION_PREFIX="${HELM_VERSION_PREFIX-}"
HELM_VERSIONS="${HELM_VERSIONS-"${HOME}/.asdf/installs/helm"}"
