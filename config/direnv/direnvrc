#!/usr/bin/env bash
# shellcheck disable=SC2181

# NOTE: all-in on asdf by default, but link directly instead of using shims
ASDF_DATA_DIR="${ASDF_DATA_DIR-${HOME}/.asdf}"

PYTHON_VERSION_PREFIX="${PYTHON_VERSION_PREFIX-}"
PYTHON_VERSIONS="${PYTHON_VERSIONS-${ASDF_DATA_DIR}/installs/python}"

NODE_VERSION_PREFIX="${NODE_VERSION_PREFIX-}"
NODE_VERSIONS="${NODE_VERSIONS-${ASDF_DATA_DIR}/installs/nodejs}"

RUBY_VERSION_PREFIX="${RUBY_VERSION_PREFIX-}"
RUBY_VERSIONS="${RUBY_VERSIONS-${ASDF_DATA_DIR}/installs/ruby}"

KUBECTL_VERSION_PREFIX="${KUBECTL_VERSION_PREFIX-}"
KUBECTL_VERSIONS="${KUBECTL_VERSIONS-${ASDF_DATA_DIR}/installs/kubectl}"

HELM_VERSION_PREFIX="${HELM_VERSION_PREFIX-}"
HELM_VERSIONS="${HELM_VERSIONS-${ASDF_DATA_DIR}/installs/helm}"

# HashiCorp products

BOUNDARY_VERSION_PREFIX="${BOUNDARY_VERSION_PREFIX-}"
BOUNDARY_VERSIONS="${BOUNDARY_VERSIONS-${ASDF_DATA_DIR}/installs/boundary}"

CONSUL_VERSION_PREFIX="${CONSUL_VERSION_PREFIX-}"
CONSUL_VERSIONS="${CONSUL_VERSIONS-${ASDF_DATA_DIR}/installs/consul}"

NOMAD_VERSION_PREFIX="${NOMAD_VERSION_PREFIX-}"
NOMAD_VERSIONS="${NOMAD_VERSIONS-${ASDF_DATA_DIR}/installs/nomad}"

PACKER_VERSION_PREFIX="${PACKER_VERSION_PREFIX-}"
PACKER_VERSIONS="${PACKER_VERSIONS-${ASDF_DATA_DIR}/installs/packer}"

SENTINEL_VERSION_PREFIX="${SENTINEL_VERSION_PREFIX-}"
SENTINEL_VERSIONS="${SENTINEL_VERSIONS-${ASDF_DATA_DIR}/installs/sentinel}"

SERF_VERSION_PREFIX="${SERF_VERSION_PREFIX-}"
SERF_VERSIONS="${SERF_VERSIONS-${ASDF_DATA_DIR}/installs/serf}"

TERRAFORM_VERSION_PREFIX="${TERRAFORM_VERSION_PREFIX-}"
TERRAFORM_VERSIONS="${TERRAFORM_VERSIONS-${ASDF_DATA_DIR}/installs/terraform}"

VAGRANT_VERSION_PREFIX="${VAGRANT_VERSION_PREFIX-}"
VAGRANT_VERSIONS="${VAGRANT_VERSIONS-${ASDF_DATA_DIR}/installs/vagrant}"

VAULT_VERSION_PREFIX="${VAULT_VERSION_PREFIX-}"
VAULT_VERSIONS="${VAULT_VERSIONS-${ASDF_DATA_DIR}/installs/vault}"

WAYPOINT_VERSION_PREFIX="${WAYPOINT_VERSION_PREFIX-}"
WAYPOINT_VERSIONS="${WAYPOINT_VERSIONS-${ASDF_DATA_DIR}/installs/waypoint}"
