#!/bin/bash
set -e

cat <<MSG
===============================================
||           Updating NPM Packages           ||
===============================================
MSG

npm install -g npm@latest yarn@latest
npm install -g diff-so-fancy gitbook-cli git-guilt eslint spoof
