#!/bin/sh
set -e

root_dir="$(git rev-parse --show-toplevel)"

if [ -z "$root_dir" ]; then
  git init
  root_dir="$(git rev-parse --show-toplevel)"
fi

if [ -d "$root_dir" ]; then
  mkdir -p "$root_dir/.git/safe"
fi
