#!/bin/sh

if [ "X$(docker ps -aq)" != "X" ]; then
  docker rm $(docker stop $(docker ps -aq))
else
  echo "No containers to cleanup"
fi

if [ "X$(docker images -qf 'dangling=true')" != "X" ]; then
  docker rmi $(docker images -qf 'dangling=true')
else
  echo "No images to cleanup"
fi
